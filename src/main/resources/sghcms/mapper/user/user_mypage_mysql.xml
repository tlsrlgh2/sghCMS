<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="userMypageDAO">

    <resultMap id="mberInfo" type="sghcms.user.service.UserMypageVO">
        <result property="mberId"         column="MBER_ID"/>
        <result property="mberNm"         column="MBER_NM"/>
        <result property="mberEmailAdres" column="MBER_EMAIL_ADRES"/>
        <result property="moblphonNo"     column="MBTLNUM"/>
    </resultMap>

    <select id="selectMberInfo" parameterType="string" resultMap="mberInfo">
        SELECT MBER_ID, MBER_NM, MBER_EMAIL_ADRES, MBTLNUM
        FROM COMTNGNRLMBER
        WHERE MBER_ID = #{mberId}
    </select>

    <update id="updateMberInfo" parameterType="sghcms.user.service.UserMypageVO">
        UPDATE COMTNGNRLMBER
        SET MBER_NM          = #{mberNm},
            MBER_EMAIL_ADRES = #{mberEmailAdres},
            MBTLNUM          = #{moblphonNo}
        WHERE MBER_ID = #{mberId}
    </update>

    <select id="selectMberPassword" parameterType="string" resultType="string">
        SELECT PASSWORD
        FROM COMTNGNRLMBER
        WHERE MBER_ID = #{mberId}
    </select>

    <update id="updateMberPassword" parameterType="map">
        UPDATE COMTNGNRLMBER
        SET PASSWORD            = #{password},
            CHG_PWD_LAST_PNTTM = sysdate()
        WHERE MBER_ID = #{mberId}
    </update>

</mapper>
